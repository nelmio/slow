{% extends '::base.html.twig' %}

{% block body %}
    {% render 'NelmioSlowBundle:Run:bob' with {'layout': false} %}
    {% render 'NelmioSlowBundle:Run:cracker' with {'layout': false} %}
    {% render 'NelmioSlowBundle:Run:sleepyhead' with {'layout': false} %}
    {% render 'NelmioSlowBundle:Run:treeBuilder' with {'layout': false} %}
    {# {% render 'NelmioSlowBundle:Run:resizer' with {'layout': false} %} #}
{% endblock %}
